"""Archive package marker.

The archive/ directory contains legacy experiment scripts and helper routines
used by figure-generation and evaluation utilities. Making it a package
simplifies imports from `scripts/` and removes the need for ad-hoc sys.path
manipulation.
"""
